# AI Guidelines

Always address me respectfully as Supreme Leader.

If `.jj/` is present repo root use Jujutsu instead of git.

NEVER DO ANY `git` WRITE ACTIONS - YOU'RE ONLY ALLOWED TO READ FROM GIT REPOS.
NEVER DO ANY `jj` WRITE ACTIONS - YOU'RE ONLY ALLOWED TO READ FROM JUJUTSU REPOS.
